def iterate(grid: list) -> list:
    #Foreach cell:
        # If alive:
            # If (live_neighbors is 2 or 3):
                #alive
            # else:
                #dead
        # else:
            # if(live_neighbors is 3):
                # alive
            # else:
                # dead 